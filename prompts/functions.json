[
	{
		"type": "function",
		"function": {
			"name": "get_web_search_result",
			"description": "Search the web to retrieve up to date information",
			"parameters": {
				"type": "object",
				"properties": {
					"query": {
						"type": "string",
						"description": "The search query to find information"
					}
				},
				"required": [
					"query"
				]
			}
		}
	},
	{
		"type": "function",
		"function": {
			"name": "get_python_math_result",
			"description": "Perform mathematical operation using python programming language. The result should be assigned to a `result` variable.",
			"parameters": {
				"type": "object",
				"properties": {
					"expression": {
						"type": "string",
						"description": "Python code that performs mathematical operation to obtain calculation result"
					}
				},
				"required": [
					"expression"
				]
			}
		}
	}
]